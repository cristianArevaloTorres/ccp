@model IEnumerable<template01.Models.usuariossocios17>
@{
    ViewBag.Title = "_obtienelcientes";
}

<ul class="feeds" >
    @{
        if (Model != null)
        {
            foreach (var item in Model)
            {
                <li>
                    <div class="col1">
                        <div class="cont" style="cursor:help" onclick="mostrarchat('@item.idUsuario')" >
                            <div class="cont-col1">
                                <div class="label label-sm label-info">
                                    <i class="fa fa-user"></i>
                                </div>
                            </div>
                            <div class="cont-col2">
                                <div class="desc">@item.nosbres    @item.ap  @item.am </div>
                            </div>
                        </div>
                    </div>
                </li>                
            }
        }

    }
</ul>





